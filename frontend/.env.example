# Frontend environment variables for Vite
# Set the API base to your backend URL. On Vercel set this to your Render backend URL.
# Example for local development:
# VITE_API_BASE=http://localhost:8000

# Example for production (Render backend):
# VITE_API_BASE=https://ecokuza.onrender.com

# If you host the frontend at https://ecokuza-teal.vercel.app and the backend at Render,
# set VITE_API_BASE to the Render backend URL in Vercel project settings.
